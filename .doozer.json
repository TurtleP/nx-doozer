{
    "targets": {
        "lovepotion": {
            "buildenv": "docker:devkitpro/devkita64",
            
            "buildcmd": "make -j${PARALLEL}",

            "environment": {
                "DEVKITPRO": "/opt/devkitpro",
                "MYBUILDID": "${REVISION}"
            },

            "artifacts": [
                "*.nro"
            ]
        }
    }
}